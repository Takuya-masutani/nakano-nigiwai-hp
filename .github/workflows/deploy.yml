name: Deploy to Xserver

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: デプロイフックを呼び出す
        run: |
          curl -f "https://nigiwaifesta.com/wp-content/themes/Custom/deploy.php?token=nigiwaifesta_deploy_2026"
